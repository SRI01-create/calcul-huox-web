# Copier ce fichier en .env.local et adapter les valeurs

# URL du backend FastAPI
# En développement local (proxy Vite actif, cette variable n'est pas nécessaire) :
VITE_API_URL=http://localhost:8000
# En production (remplacer par l'URL Render) :
# VITE_API_URL=https://calcul-huox-api.onrender.com
